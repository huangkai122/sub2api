# 2026-07-31

- 凯哥在 Windows PowerShell 的 `frontend` 目录执行 Go build，首次因不在 Go module 根目录报错；之后已在 `backend` 下生成 `bin/server`。提醒该产物若未设置 `GOOS=linux GOARCH=amd64`，默认是 Windows 可执行文件，不能直接部署 Linux。上线需重新交叉编译嵌入前端的 Linux 产物，并采用上传临时文件、备份旧二进制、停服替换、启动及 HTTP health 验证流程。
- 生产 systemd `ExecStart` 已确认是 `/opt/sub2api/sub2api`；新上传文件为 `/opt/sub2api/server-linux-amd64`，scp 后是 root:root 且无执行位。正确替换方式：先 `file` 验证 ELF x86-64，备份旧 `/opt/sub2api/sub2api`，停服务，再用 `install -o sub2api -g sub2api -m 0755` 覆盖，启动并用明文 HTTP `/health` 验证。
- 完成控制台内置 API 文档页：新增 `/api-docs` 和 `ApiDocsView.vue`，按百炼 Qwen、MIMO、方舟 Ark 展示最小 curl 调用；Qwen/Ark 覆盖文本、图片、文生视频、图生视频，MIMO 仅文本。侧栏内置入口会过滤生产中同名但已失效的 localhost:5175 自定义菜单，避免重复。类型检查、ESLint、145 个测试文件共 938 项测试、临时目录生产构建均通过；正式 dist 未覆盖。
