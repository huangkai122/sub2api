# 2026-07-30

- 凯哥问"代码修改后怎么打包再上线"。整理标准流程：前端 `pnpm --dir frontend run build` → 后端 `CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build -tags=embed -ldflags="-s -w" -trimpath -o bin/server-linux-amd64 ./cmd/server`（**必须 -tags=embed**，否则 embed_off.go 让所有前端路由 404）→ scp 覆盖生产二进制 → `systemctl restart sub2api`。已写入 MEMORY.md「部署打包标准流程」。
- 仍挂的待办：生产 API 文档页 `/custom/<id>` 空白——前端框架已渲染（菜单/标题正常），怀疑拉文档内容的后端接口无数据或报错；凯哥贴的 Network 只见到 stripe 埋点（m.stripe.com，无关，是 Stripe.js telemetry）。需凯哥在浏览器 Network→Fetch/XHR 里看 custom 文档接口的状态码/响应定位；或由我查源码定位该接口路径。上次问的，尚未回。
