# 2026-08-05

- 根据生产截图确认 `/api-docs` 返回前端 404、侧栏无入口。生产公开配置版本为 `0.1.152`，`custom_menu_items=[]`；本地源码已包含 `/api-docs` 路由和内置侧栏入口，本地嵌入 dist 也已有 ApiDocsView。结论是生产二进制仍为未包含该前端页面的旧构建，需要用当前前端 dist 重新执行带 `-tags=embed` 的 Linux 后端编译并部署，后台无需配置自定义菜单。
- 按凯哥要求将侧栏内置“API文档”入口从“API密钥”下方移动到“个人资料”正下方，其他菜单顺序保持不变。
