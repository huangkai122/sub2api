# 2026-07-29

- 排查 Linux systemd 裸机部署失败：AUTO_SETUP 实际读取 `DATABASE_HOST/PORT/USER/PASSWORD/DBNAME/SSLMODE`，不是 Docker Compose 常用的 `POSTGRES_*`；使用 `POSTGRES_USER/PASSWORD` 会回退为 postgres/空密码。systemd 的 `Environment=` 不执行 `$(openssl rand -hex 32)`，必须先在 shell 生成值再写入。生产裸机建议设置 `DATA_DIR=/opt/sub2api/data`。
- 将 `deploy/sub2api.service` 改为生产模板：敏感配置统一由 `/etc/sub2api/sub2api.env` 加载，监听和数据库参数不再硬编码；改用 `Restart=on-failure`、network/docker 启动顺序，并保留 systemd 安全加固和 `/opt/sub2api` 写权限。
- 服务器已成功加载 `/etc/sub2api/sub2api.env`（Data directory 正确）且 PostgreSQL 连接成功；AUTO_SETUP 下一步卡在 Redis `NOAUTH Authentication required`，需把 Redis 实际密码写入 `REDIS_PASSWORD` 后重启。
- 后端已正常启动；核对 `backend/bin/server-linux-amd64` 为 linux/amd64 静态二进制（约 103MB），前端 dist 存在且生产编译流程使用 `-tags=embed`，因此前端应由同一 8080 服务直接提供，无需单独部署静态站点，只需验证 `/` 后配置域名 HTTPS 反代。
- 生产服务器使用 Nginx/OpenResty 反向代理（非 Caddy）；代理 Sub2API 时需兼顾 SSE/流式 AI 响应与媒体上传：HTTP/1.1、关闭 proxy buffering、长 read/send timeout、请求体上限与后端 256MB 对齐。
- `https://maas.shandiansha.com/` 公网返回 OpenResty 502。若 OpenResty 由 Docker/1Panel 运行，`proxy_pass 127.0.0.1:8080` 会指向代理容器自身；应让 Sub2API 监听 `0.0.0.0:8080`，代理到 OpenResty 所在 Docker 网络的宿主机网关（须用 inspect 确认），并用防火墙限制 8080。
- 公网复测 `/health` 已 200，但 `/assets/*.js|css` 全部仍由 OpenResty 返回 502，确认是独立 `location /assets/` 使用了错误 upstream；应删除该 location 统一走主反代，或改成同一 Docker 网关。浏览器同时报 CSP nonce，需禁止首页代理缓存且不要由 OpenResty覆盖 CSP 响应头，避免缓存 HTML nonce 与当前响应头 nonce 不一致。
- 本地数据库迁生产应使用 PostgreSQL custom dump (`pg_dump -Fc`) 和 `pg_restore`，生产恢复前先备份并停 Sub2API；Redis无需迁移。数据库包含以 `TOTP_ENCRYPTION_KEY`（AES-256-GCM）加密的敏感值，生产必须同步本地原密钥，否则迁移后的账号密钥/TOTP/加密设置无法解密；JWT_SECRET 可保留生产独立值。
- 用户已在 `C:\Users\Administrator\Desktop\sub2api` 导出 80+ 个按表 SQL。只读抽查确认它们是带显式列名的纯 `INSERT` 数据文件，不含 DDL；其中 `accounts.sql` 含明文上游 API 凭据。若使用这些文件迁移，必须对生产先备份/停应用/清空表，导入时处理外键，完成后修复所有序列；更推荐重新导出单个 `pg_dump -Fc`。
- 只读审查 `C:\Users\Administrator\Desktop\public.sql`：Navicat PostgreSQL 16 schema+data 文本导出，含 77 张表、约 20,907 条 INSERT、序列 setval、索引约束，但无 BEGIN/COMMIT；且错误手工导出了 pg_trgm 内部对象（文件开头先创建依赖尚未定义函数的 `public.gtrgm`，后续含 `$libdir/pg_trgm` 函数和 `gin_trgm_ops`）。不应原样直接导入生产，推荐重新 `pg_dump -Fc`；若必须使用，先在临时空库验证并剔除扩展内部对象，生产恢复前备份和停应用。
- Navicat 直接数据传输实测失败：创建 `settings` 表时默认值引用 `settings_id_seq`，但目标端序列尚未创建，报 `relation settings_id_seq does not exist`；说明其对象迁移顺序不可靠，不应继续用结构+数据直传，改用容器内 `pg_dump -Fc` / `pg_restore`。
- 本地 PostgreSQL 容器确认为 `postgres`，镜像 `postgis/postgis:16-3.4`、PG 16.4，业务库 `sub2api`；Windows CMD 不支持 Bash 的 `\` 续行和 `grep`，相关 docker/pg_dump 命令须写成单行或改用 CMD 自带 `findstr`。容器 PGDATA 实际由匿名卷挂载到 `/var/lib/postgresql/data`，外层 `G:/postgres/data:/var/lib/postgresql` 并非实际 PGDATA。
- 本地 `sub2api.dump` 已用 `pg_restore --list` 验证：Custom 格式、PG 16.4、966 TOC 条目，包含 `pg_trgm` 扩展、表、序列和依赖。Git Bash 下 Windows G 盘应写 `/g/postgres/sub2api.dump` 或 `G:/postgres/sub2api.dump`；`G:\...` 会被反斜杠转义。容器内路径与宿主机路径必须通过 `docker cp` 明确转换。
- 在 Windows Git Bash 调用 `docker exec ... -f /tmp/sub2api.dump` 时，MSYS 会把容器路径自动转换为 Windows `%TEMP%` 路径，导致 pg_dump 无法写入。需用 `MSYS_NO_PATHCONV=1 docker exec ...`（或在 CMD/PowerShell 执行）禁用路径转换；`docker cp` 的 Windows 目标路径仍用 `G:/...`。
- 本地标准归档已产出：`G:/postgres/sub2api.dump`，约 1.3MB，SHA256 `050c9d15adaff4f04304dabf9a0a3850a0a0dba756409b72fb998482f205d61a`。下一步上传生产后先校验哈希并确认生产 PostgreSQL 容器名/版本/管理员用户，再做生产备份、停服务和恢复。
- 生产恢复命令已交付：先同步本地 `TOTP_ENCRYPTION_KEY` 到 `/etc/sub2api/sub2api.env`（否则加密字段无法解密），再备份生产库、`systemctl stop sub2api`、用 `pg_restore --clean --if-exists --no-owner` 从 `/root/sub2api.dump` 恢复，最后启动并用 `/health` 验证；dump 含 `pg_trgm` 扩展（postgres:16 官方镜像自带 contrib 可建），但若本地库依赖 PostGIS 类型而生产为纯 `postgres:16` 镜像则会失败，需提前确认。
- 实测 `pg_restore --clean` 以 `sub2api` 连接报 `must be owner of table account_groups`（生产已有对象 owner 非 sub2api，DROP 无权限）。改用更干净方案：停应用后 `DROP DATABASE sub2api WITH (FORCE)` 重建空库 `OWNER sub2api`，再 `pg_restore --no-owner`（不加 --clean）纯导入，对象 owner 直接归 sub2api，无需 REASSIGN；本地数据已在 `/root/sub2api_before_*.dump` 备份可回滚。
- 应用启动报 `apply migration 109_auth_identity_compat_backfill.sql: value too long for type character varying(40)`：根因非数据，而是 `auth_identity_migration_reports.report_type` 在 `108` 定义为 `VARCHAR(40)`，而 `109` 插入的字面量 `oidc_synthetic_email_requires_manual_recovery` 长 45 字符超限（其他 report_type 均 <40）。上游用 `108a_widen_*` 和 `121_*` 两迁移把列改 `VARCHAR(80)`，但若生产二进制/恢复库未应用 `108a` 则不生效。修复：停服务后 `ALTER TABLE auth_identity_migration_reports ALTER COLUMN report_type TYPE VARCHAR(80);` 再启动；与上游定义一致，后续 121 再 ALTER 同样无害。本质问题是本地库版本落后、生产二进制较新导致补跑迁移踩到源码已知列宽 bug。
