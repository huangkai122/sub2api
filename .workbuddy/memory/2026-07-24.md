# 2026-07-24

## 任务：渠道监控（admin/channels/monitor）新增 Qwen/MiMo/Ark 三个平台

### 背景
- 项目正在做 qwen/mimo/ark 平台支持（账号/分组/限额，凯哥自己在提交，提交名 "mimo/ark"），监控模块是补齐的一环。
- 三平台均为 OpenAI 兼容协议（Bearer + choices.0.message.content），网关里走 OpenAI 转发路径。

### 关键决策（凯哥拍板）
- **endpoint 支持带路径**：填了 base path（如 `https://ark.cn-beijing.volces.com/api/plan/v3`）就只追加协议后缀 `/chat/completions`；只填 origin 时用平台默认路径；路径已以后缀结尾则原样使用。与账号 base_url 语义对齐，向后兼容。
- 平台默认路径：qwen=`/compatible-mode/v1/chat/completions`，mimo=`/v1/chat/completions`，ark=`/api/v3/chat/completions`。

### 改动清单
后端：
- `channel_monitor_const.go`：MonitorProviderQwen/Mimo/Ark 常量 + 路径常量 + 错误文案。
- `channel_monitor_checker.go`：providerAdapter 加 `pathSuffix` 字段；`openAICompatibleChatAdapter(defaultPath)` 工厂复用 OpenAI chat 实现；`resolveRequestURL()` 替代直接 joinURL；merge 黑名单加三平台。
- `channel_monitor_validate.go`：validateEndpoint 放行 path（仍禁 query/fragment、强制 https、SSRF 检查不变）。
- ent schema 两表 provider 枚举 + `go generate ./ent` 重生成。
- `migrations/175_channel_monitor_add_qwen_mimo_ark_providers.sql`：DROP IF EXISTS + ADD 重建两张表 provider CHECK 约束（幂等）。
- handler 两处 oneof、模板错误文案。
- 新增 `channel_monitor_checker_provider_test.go`：三平台端到端（fake server 过 challenge）、base path 拼接、resolveRequestURL 全分支、validate、merge 黑名单。
- 顺手修了 WIP 遗留的红测试：`user_platform_quota_admin_test.go`（8 平台）、`api_contract_test.go` default_platform_quotas 夹具（8 平台）。这两个修复还在工作区未提交（凯哥自己提交代码，不代劳）。

前端：
- Provider 类型、constants、`useChannelMonitorFormat`（badge/picker/gradient：qwen=violet, mimo=rose, ark=cyan）、ProviderIcon（复用 PlatformIcon 路径）、MonitorFormDialog/MonitorFiltersBar/MonitorTemplateManagerDialog 选项、zh/en i18n、`channel/types.ts` 平台颜色。
- 表单 endpoint 下加 endpointHint 文案说明可填完整 base URL。
- 修 `settings.authSourceDefaults.spec.ts` 全 null map 5→8 平台（WIP 遗留）。

### 验证
- 后端 `go build ./...` + `go test -tags=unit ./...`：45 包 ok，仅 internal/config 两个测试失败。
- **已用基线 worktree（a1930ea6）证实 config 的 TestLoadForBootstrapAllowsMissingJWTSecret / TestLoadDefaultDatabaseSSLMode 是历史遗留失败**，与本次无关。
- 前端 `vue-tsc --noEmit` 干净；vitest 932/932 通过。

### 环境发现
- Go 1.26.5 在 `/e/Program Files/Go/bin/go.exe`（git bash PATH 没有；PowerShell 工具输出异常不可用）。
- 凯哥工作方式：自己频繁 commit（看到改动被收进 "mimo/ark" 提交），我留工作区修改给他提交，不代 commit。
